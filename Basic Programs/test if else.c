#include<stdio.h>
void main(){
    int m, n;
    printf("Enter the value of m:");
    scanf("%d",&m);
    if(m!=0)
        if(m>=0)
         else
        n=1;
         else
        n=-1;
        n=0;
        printf("The value of m=%d\n",m);
        printf("The vslue of n=%d\n",n);

  return 0;
}
