#include<stdio.h>
int main(){
    int x, y, result;
    printf("Input a integer for x :");
    scanf("%d", &x);
    printf("Input a integer for y :");
    scanf("%d", &y);
    result=x*y;
    printf("\nThe product is=  %d", result);
    return 0;
}