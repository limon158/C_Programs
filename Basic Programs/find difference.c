//Distance between two point (x1, x2) and (y1, y2) is govornedn by the formula
#include<stdio.h>
int main()
{
int x1, x2, y1, y2;
double D;
 printf("Input the value for x1 :");
 scanf("%d", &d);
 printf("Input the value for x2 : ");
 scanf("%d", &d);
 priintf("Input the value for ");
 scanf("%d", &d);
 print("%")

printf()
}
