 //Find the unique elements in an aray
 #include<stdio.h>
 #define N 100
 int main()
 {
    int i, j, k;
    int row1, column1;
    int matrixA[N][N], mstrix2[N][N], i_mat[N][N]={{1, 0, 0}, {0, 1, 0}, {0, 0, 1}}, mat_AB[N][N], mat_BA[N][N];
    for(i=0; i<row1; i++)
    {
      for(j=0; j<column1; j++)
      {
        printf("%d\t", i_mat[i][j]);
      }
    }

return 0;
 }